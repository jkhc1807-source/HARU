# 하루여행 프로젝트 작업 안내

이 저장소에서 작업을 시작할 때 다음 문서를 먼저 읽는다.

1. `docs/PROJECT_CONTEXT.md` — 현재 상태, 기술 구성, 배포 정보, 주의점
2. `docs/PRODUCT.md` — 제품 목표와 사용자 흐름
3. `docs/ROADMAP.md` — 완료 항목과 다음 작업

## 작업 원칙

- 사용자는 비개발자이므로 결과와 사용법을 쉬운 한국어로 설명한다.
- 이 앱은 수익 목적이 아닌 토이 프로젝트다. 유료 API나 과도한 인프라는 사용자 동의 없이 추가하지 않는다.
- 장소 데이터는 카카오맵 JavaScript SDK의 장소 검색을 사용한다.
- API 키 원문은 문서나 Git에 기록하지 않는다. 로컬에서는 `.env.local`을 사용한다.
- 요청받은 일반적인 수정은 합리적인 범위에서 바로 구현한다. 결제, 공개 범위 변경, 데이터 삭제, 새로운 유료 서비스처럼 영향이 큰 작업만 실행 전에 확인한다.
- 기능 변경 후 영향 범위에 맞는 검증을 수행한다. 배포 가능한 변경은 `npm run build`로 확인하고 기존 Sites 프로젝트에 새 버전을 배포한다.
- 배포 사이트의 공개 범위나 도메인을 임의로 변경하지 않는다.
- 기능을 완료하거나 중요한 결정을 내리면 `docs/PROJECT_CONTEXT.md`와 `docs/ROADMAP.md`도 갱신한다.

## 구현 기준

- 요청을 만족하는 가장 단순한 구조를 우선한다. 요청하지 않은 기능과 성급한 추상화를 추가하지 않는다.
- 수정 범위를 작게 유지하고 관련 없는 코드, 디자인, 프로젝트 구조를 함께 바꾸지 않는다.
- 중첩 조건이 복잡해질 때는 early return을 우선 검토하되 기계적으로 강제하지 않는다.
- 컴포넌트나 함수가 여러 책임을 갖기 시작하면 화면, 검색, 일정 계산, 지도 표현 단위로 분리한다. 줄 수만으로 불필요하게 쪼개지 않는다.
- 이벤트 함수는 가능하면 `handle...`, 불리언 상태는 `is...`, `has...`, `show...`처럼 의미가 드러나는 이름을 사용한다. 기존 코드와 충돌하면 일관성을 우선한다.
- React 훅의 의존성과 비동기 상태를 점검해 stale closure, 중복 요청, 언마운트 후 상태 변경을 방지한다.
- 임시 `console.log`, `debugger`, 테스트용 UI는 완료 전에 제거한다.

## UI와 CSS 기준

- 현재 프로젝트의 `app/globals.css`와 기존 클래스·디자인 토큰을 우선 재사용한다. Tailwind나 SCSS 같은 새 스타일 체계를 임의로 도입하지 않는다.
- 공통 스타일 수정 전 다른 화면과 반응형 구간에 미치는 영향을 확인한다. 필요한 경우 기능별 래퍼나 한정된 클래스로 범위를 좁힌다.
- CSS 선언은 가능하면 레이아웃, 크기·간격, 타이포그래피, 시각 효과 순으로 정리한다.
- `!important`는 기본 해결책으로 사용하지 않는다. 명시도나 구조로 해결하기 어려운 기존 스타일 충돌에만 제한적으로 사용한다.
- 모바일 320px 이상과 데스크톱에서 가로 넘침, 잘림, 버튼 축소, 긴 장소명·주소 줄바꿈을 확인한다.
- 고정 크기가 필요한 flex 자식에는 상황에 맞게 `flex-shrink: 0`과 최소 크기를 적용한다.
- 버튼과 입력 등 터치 요소는 누르기 쉬운 크기와 명확한 포커스 상태를 제공한다.

## 접근성과 상호작용 기준

- 의미에 맞는 HTML 요소를 사용하고 입력에는 label 또는 접근 가능한 이름을 제공한다.
- 아이콘만 있는 버튼에는 `aria-label`, 정보성 이미지에는 적절한 `alt`를 제공한다.
- 키보드만으로 핵심 흐름을 사용할 수 있어야 하며, 보이지 않는 요소가 포커스를 가져가지 않게 한다.
- 모달을 추가하면 열릴 때 내부 포커스 이동, 닫힐 때 트리거로 복귀, Escape 닫기와 포커스 고정을 함께 구현한다.
- 검색·저장·생성 중에는 로딩 상태를 보여주고 제출 버튼을 비활성화해 중복 실행을 막는다.
- 성공, 결과 없음, API 오류, 설정 누락을 서로 다른 문구로 안내한다.
- 빈 결과, 긴 텍스트, 느린 응답, 모바일 화면을 포함한 경계 사례를 확인한다.

## 검증과 보고 기준

- 변경 전 관련 코드를 읽고 데이터 흐름과 사용자 동작을 먼저 파악한다.
- 수정 후 최소한 타입·빌드 오류와 변경한 핵심 흐름을 확인한다. 화면 동작이 핵심이면 브라우저로 실제 입력부터 결과까지 재현한다.
- 기존 테스트가 실패하면 이번 변경 때문에 생긴 실패인지 오래된 테스트인지 구분해 보고한다.
- 사용자에게는 먼저 결과를 말하고, 변경점·확인 방법·남은 제약만 간결하게 설명한다.
- 사용자의 제안에 더 안전하거나 단순한 대안이 있으면 근거와 함께 제안한다. 무조건 동의하거나 불필요하게 작업을 멈추지 않는다.
